// Root build.gradle: shared config for every subproject.
// Module-specific dependencies/plugins live in common/build.gradle and fabric/build.gradle.
allprojects {
	group = rootProject.group
	version = rootProject.version
}
